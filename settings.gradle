pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'https://jitpack.io'
            credentials {
                username = System.getenv('luutugg')
                password = System.getenv('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')
            }
        }
    }
}
rootProject.name = "DemoPush"
include ':app'
include ':mylibrary'
